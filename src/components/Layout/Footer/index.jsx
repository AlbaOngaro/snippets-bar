import React from "react";
import styled from "styled-components";

import styles from "./styles";

const Footer = styled.div`
  ${styles.Footer}
`;

export default (props) => <Footer {...props}></Footer>;
