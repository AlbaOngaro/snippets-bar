import React from "react";
import styled from "styled-components";

import styles from "./styles";

const Header = styled.div`
  ${styles.Header}
`;

export default (props) => <Header {...props}></Header>;
